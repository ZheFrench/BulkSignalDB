import { RouterOptions } from 'express';
import { Type } from '../middleware';
/**
 * Registers controller for base url
 *
 * @param {string} url
 * @param {Type[]} [middleware]
 */
export declare function Controller(url: string, middleware?: Type[]): any;
/**
 * Registers controller for base url
 *
 * @param {string} url
 * @param routerOptions
 * @param {Type[]} [middleware]
 */
export declare function Controller(url: string, routerOptions?: RouterOptions, middleware?: Type[]): any;
