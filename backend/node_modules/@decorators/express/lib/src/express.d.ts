import { Router, Express } from 'express';
import { Type } from './middleware';
/**
 * Attach controllers to express application
 *
 * @param {Express} app Express application
 * @param {Type[]} controllers Controllers array
 */
export declare function attachControllers(app: Express | Router, controllers: Type[]): void;
/**
 * Attach controller instances to express application
 *
 * @param {Express} app Express application
 * @param {any[]} controllers Controllers array
 */
export declare function attachControllerInstances(app: Express | Router, controllers: object[]): void;
