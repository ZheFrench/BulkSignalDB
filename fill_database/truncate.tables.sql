use SingleCell;

SET FOREIGN_KEY_CHECKS = 0;
TRUNCATE `release`;
TRUNCATE `feature_release`;
TRUNCATE `component`;
TRUNCATE `feature_component`;
TRUNCATE `interaction`;
TRUNCATE `interactor`;
SET FOREIGN_KEY_CHECKS = 1;

